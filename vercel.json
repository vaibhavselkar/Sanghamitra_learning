{
  "version": 2,
  "name": "sanghamitra-learning",
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build",
        "installCommand": "cd frontend && npm install",
        "buildCommand": "cd frontend && CI=false npm run build"
      }
    },
    {
      "src": "server/app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/app.js",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    },
    {
      "src": "/static/(.*)",
      "dest": "/frontend/build/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/frontend/build/$1"
    },
    {
      "src": "/(.*)",
      "status": 404,
      "dest": "/frontend/build/index.html"
    }
  ],
  "env": {
    "DATABASE": "mongodb+srv://sanghamitralearn:sanghamitra%4007@platform.tzqhhat.mongodb.net/sanghamitralearning?retryWrites=true&w=majority&appName=Platform",
    "SECRET_KEY": "SANGHAMITRAISBUILDINGLEARNINGPLATFORM",
    "PORT": "4000"
  }
}
